/**
 * Knowledge Search Panel
 *
 * A 3-tab interface for RAG (Retrieval-Augmented Generation) functionality:
 * - Search: Query knowledge base, view results, pin items
 * - Settings: Configure RAG parameters (quality, generation, citations, etc.)
 * - Context: Manage attached tabs, files, and pinned results
 */

export { KnowledgePanel } from "./KnowledgePanel"
export type { KnowledgePanelProps } from "./KnowledgePanel"
